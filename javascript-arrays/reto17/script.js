// Escribe una función que tome una matriz (a) y un número (b) como argumentos
// Sumar todos los elementos de la matriz con un valor mayor que b
// Devuelve la suma

function sumElementsWithValueGreater(a, b) {
  for (let i = 0; i < a.length; i++) {}
}
// no entendi
